{
  "name": "imgpact",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "jsdom": "^29.0.1"
  }
}
